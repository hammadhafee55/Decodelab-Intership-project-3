# Decodelab-Intership-project-3
This project uses SQL queries to analyze a dataset. Commands like SELECT, WHERE, and LIKE are used to filter data. Functions like COUNT(), SUM(), and AVG() help in calculations. GROUP BY and ORDER BY are used to organize and sort the results.
